## Do NOT delete! Keep this line to avoid the native2ascii UTF-8 BOM bug. See #APF-639

# -- validator errors --
errors.required=${getText(fieldName)} dien muc la bat buoc. 
errors.requiredField={0}  dien muc la bat buoc.
errors.date=${getText(fieldName)}  khong phai dinh dang ngay thang hop le.
errors.email=${email}  dia chi e -mail khong hop le.
errors.phone=${phoneNumber}  so dien thoai khong hop le.
errors.zip=${postalcode}  ma buu dien khong hop le.
errors.twofields= xac nhan mat ma va mat ma phai trung nhau.